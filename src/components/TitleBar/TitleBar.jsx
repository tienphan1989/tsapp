import React, { Component } from 'react'
import { Container } from '@material-ui/core';

export default class TitleBar extends Component {
    render() {
        return (
            <Container>
                Hello this is the title bar goes
            </Container>
        )
    }
}
